# Opcional: o app já aponta para o Worker de produção sem este arquivo.
# Copie para .env.local só se quiser outra URL.

# Produção (padrão no código):
# EXPO_PUBLIC_API_URL=https://embarqueai-api.gabrielviniciusadorno.workers.dev
# Worker local: http://localhost:8787  (Android emulador: http://10.0.2.2:8787)
# Mock local: mock
# EXPO_PUBLIC_API_URL=mock

# Maps SDK (iOS/Android). Não use a chave de servidor aqui.
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=
