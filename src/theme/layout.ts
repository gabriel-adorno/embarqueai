export const PAGE_PADDING = 24;

export const floatingTabBar = {
  height: 62,
  sideInset: 20,
  extraBottom: 10,
} as const;

/** Extra space at the end of screens that sit above the floating tab bar. */
export const TAB_SCREEN_BOTTOM = 118;
